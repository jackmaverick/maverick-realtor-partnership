---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CTAButton from '../../components/CTAButton.astro';
import locations from '../../data/locations.json';
import services from '../../data/services.json';

export async function getStaticPaths() {
  return locations.map((location) => ({
    params: { location: location.slug },
    props: { location },
  }));
}

const { location } = Astro.props;
---

<BaseLayout
  title={`Roofing Services in ${location.name}, ${location.state}`}
  description={`Professional roofing services for realtors in ${location.name}, ${location.state}. ${location.roofsCompleted}+ roofs completed. Fast inspections, payment at closing available.`}
>
  <!-- Hero -->
  <section class="bg-gradient-to-b from-primary-50 to-white py-16 md:py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
          Expert Roofing Services in {location.name}, {location.state}
        </h1>
        <p class="text-xl text-gray-600 mb-6">
          Serving real estate professionals in {location.name} with fast inspections, competitive pricing, and 30+ years of roofing expertise.
        </p>
        <div class="flex flex-col sm:flex-row gap-4">
          <CTAButton href="/contact" variant="primary" size="lg">
            Request Inspection
          </CTAButton>
          <CTAButton href="tel:9132686052" variant="outline" size="lg">
            Call (913) 268-6052
          </CTAButton>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats -->
  <section class="py-12 bg-white border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
        <div class="text-center">
          <div class="text-4xl font-bold text-primary-600 mb-2">{location.roofsCompleted}+</div>
          <div class="text-sm text-gray-600">Roofs Completed</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-primary-600 mb-2">30+</div>
          <div class="text-sm text-gray-600">Years Experience</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-primary-600 mb-2">{location.medianHomeValue}</div>
          <div class="text-sm text-gray-600">Median Home Value</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-primary-600 mb-2">A+</div>
          <div class="text-sm text-gray-600">BBB Rating</div>
        </div>
      </div>
    </div>
  </section>

  <!-- About Location -->
  <section class="py-16 md:py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid lg:grid-cols-2 gap-12">
        <div>
          <h2 class="text-3xl font-bold text-gray-900 mb-6">
            Why {location.name} Realtors Choose Maverick Exteriors
          </h2>
          <div class="prose prose-lg text-gray-600">
            <p>{location.description}</p>

            <p>
              For over 30 years, RealtorRoof has been the trusted <a href={`/locations/${location.slug}/roof-replacement`} class="text-violet-600 hover:text-violet-700 font-semibold underline">roofing partner</a> for real estate professionals throughout {location.name}, {location.state}. We understand the unique challenges that realtors face when navigating roof issues during real estate transactions, and we've built our <a href={`/locations/${location.slug}/roof-inspections`} class="text-violet-600 hover:text-violet-700 font-semibold underline">inspection</a> and <a href={`/locations/${location.slug}/roof-repair`} class="text-violet-600 hover:text-violet-700 font-semibold underline">repair services</a> specifically to support your needs in {location.name}.
            </p>

            <p>
              Whether you're representing a buyer who needs a fast, accurate <a href={`/locations/${location.slug}/roof-inspections`} class="text-violet-600 hover:text-violet-700 font-semibold underline">roof inspection in {location.name}</a> to meet closing deadlines, or you're working with a seller who wants to maximize their home's value with a <a href={`/locations/${location.slug}/roof-replacement`} class="text-violet-600 hover:text-violet-700 font-semibold underline">new roof replacement</a>, we have the expertise and flexibility to help you close more deals in {location.name}.
            </p>

            <h3 class="text-2xl font-bold text-gray-900 mt-8 mb-4">Our {location.name} Roofing Services for Realtors:</h3>
            <ul class="space-y-3">
              <li><strong><a href={`/locations/${location.slug}/roof-inspections`} class="text-violet-600 hover:text-violet-700 underline">Roof Inspections in {location.name}</a>:</strong> 24-48 hour turnaround for detailed roof assessments that keep your real estate transactions on track</li>
              <li><strong><a href={`/locations/${location.slug}/roof-replacement`} class="text-violet-600 hover:text-violet-700 underline">Roof Replacement Services</a>:</strong> Payment at closing options available to help your {location.name} buyers manage their budget during the home purchase process</li>
              <li><strong><a href={`/locations/${location.slug}/roof-repair`} class="text-violet-600 hover:text-violet-700 underline">Roof Repairs in {location.name}</a>:</strong> Fast scheduling to address inspection issues and meet closing deadlines for your clients</li>
              <li><strong><a href={`/locations/${location.slug}/insurance-claims`} class="text-violet-600 hover:text-violet-700 underline">Insurance Claims Assistance</a>:</strong> We work directly with adjusters to navigate the complex world of roof insurance coverage in {location.name}</li>
              <li><strong><a href={`/locations/${location.slug}/pre-listing-services`} class="text-violet-600 hover:text-violet-700 underline">Pre-Listing Roof Services</a>:</strong> Help {location.name} sellers maximize property value with proactive roof assessments and improvements</li>
            </ul>
          </div>
        </div>

        <div>
          <div class="bg-gray-50 rounded-2xl p-8 border border-gray-200 mb-8">
            <h3 class="text-2xl font-bold text-gray-900 mb-4">Climate & Roofing in {location.name}</h3>
            <p class="text-gray-600 mb-6">{location.climate}</p>

            <h4 class="text-lg font-semibold text-gray-900 mb-3">Common Roofing Issues We Address:</h4>
            <ul class="space-y-2">
              {location.commonIssues.map((issue) => (
                <li class="flex items-start">
                  <svg class="w-5 h-5 text-primary-600 mr-2 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span class="text-gray-700">{issue}</span>
                </li>
              ))}
            </ul>
          </div>

          <div class="bg-primary-50 rounded-2xl p-8 border border-primary-100">
            <h3 class="text-2xl font-bold text-gray-900 mb-4">Popular {location.name} Neighborhoods</h3>
            <p class="text-gray-600 mb-4">We proudly serve realtors and homeowners throughout {location.name}, including:</p>
            <div class="flex flex-wrap gap-2">
              {location.neighborhoods.map((neighborhood) => (
                <span class="px-3 py-1 bg-white text-gray-700 rounded-full text-sm border border-primary-200">
                  {neighborhood}
                </span>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Available in This Location -->
  <section class="py-16 md:py-24 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          Our Services in {location.name}, {location.state}
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Click any service to learn more about our {location.name}-specific offerings
        </p>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        {services.map((service) => {
          const colors = [
            { gradient: 'from-violet-500 to-purple-600', hover: 'hover:from-violet-600 hover:to-purple-700', text: 'text-violet-100' },
            { gradient: 'from-pink-500 to-rose-600', hover: 'hover:from-pink-600 hover:to-rose-700', text: 'text-pink-100' },
            { gradient: 'from-blue-500 to-cyan-600', hover: 'hover:from-blue-600 hover:to-cyan-700', text: 'text-blue-100' },
            { gradient: 'from-orange-500 to-amber-600', hover: 'hover:from-orange-600 hover:to-amber-700', text: 'text-orange-100' },
            { gradient: 'from-indigo-500 to-blue-600', hover: 'hover:from-indigo-600 hover:to-blue-700', text: 'text-indigo-100' },
          ];
          const colorIndex = services.indexOf(service) % colors.length;
          const color = colors[colorIndex];

          return (
            <a
              href={`/locations/${location.slug}/${service.slug}`}
              class={`group block bg-gradient-to-br ${color.gradient} ${color.hover} rounded-2xl p-6 shadow-lg transition-all`}
            >
              <div class="text-4xl mb-4">{service.icon}</div>
              <h3 class="text-xl font-bold text-white mb-2">{service.name}</h3>
              <p class={`${color.text} text-sm mb-4 leading-relaxed`}>
                {service.shortDescription}
              </p>
              <div class="flex items-center text-white font-semibold">
                Learn More
                <svg class="w-5 h-5 ml-2 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  </section>

  <!-- Realtor Benefits -->
  <section class="py-16 md:py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          How We Help {location.name} Realtors Close More Deals
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Our services are specifically designed to support real estate professionals and keep your transactions moving forward.
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-8">
        <div class="bg-gray-50 rounded-lg p-6 shadow-sm border border-gray-200">
          <div class="h-12 w-12 bg-violet-100 rounded-lg flex items-center justify-center mb-4">
            <svg class="h-6 w-6 text-violet-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">Fast Response Times</h3>
          <p class="text-gray-600">
            We understand real estate timelines. Get roof inspections scheduled within 48 hours and reports delivered in days, not weeks.
          </p>
        </div>

        <div class="bg-gray-50 rounded-lg p-6 shadow-sm border border-gray-200">
          <div class="h-12 w-12 bg-pink-100 rounded-lg flex items-center justify-center mb-4">
            <svg class="h-6 w-6 text-pink-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 9V7a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2m2 4h10a2 2 0 002-2v-6a2 2 0 00-2-2H9a2 2 0 00-2 2v6a2 2 0 002 2zm7-5a2 2 0 11-4 0 2 2 0 014 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">Flexible Payment Options</h3>
          <p class="text-gray-600">
            Payment at closing available for buyers who need budget flexibility during the home purchase process.
          </p>
        </div>

        <div class="bg-gray-50 rounded-lg p-6 shadow-sm border border-gray-200">
          <div class="h-12 w-12 bg-blue-100 rounded-lg flex items-center justify-center mb-4">
            <svg class="h-6 w-6 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">Price Matching Promise</h3>
          <p class="text-gray-600">
            Show us a comparable bid from a licensed contractor and we'll match it. No negotiation needed.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- SEO Content -->
  <section class="py-16 md:py-24 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="prose prose-lg max-w-none">
        <h2>Expert Roofing for {location.name} Real Estate Transactions</h2>
        <p>
          When you're managing real estate transactions in {location.name}, having a reliable roofing partner can make the difference between a smooth closing and a deal that falls apart. Maverick Exteriors has been serving {location.name} realtors for over three decades, and we understand exactly what you need to keep your transactions moving forward.
        </p>

        <h3>Understanding {location.name}'s Roofing Market</h3>
        <p>
          With a median home value of {location.medianHomeValue}, {location.name} represents a significant investment for your clients. The roofing component of these homes is critical to both property value and buyer confidence. Our team has completed over {location.roofsCompleted} roofs in {location.name}, giving us deep expertise in the specific challenges and opportunities this market presents.
        </p>

        <h3>Comprehensive Roofing Services in {location.name}, {location.state}</h3>
        <p>
          Unlike general roofing contractors, we've built our business model specifically to support realtors in {location.name}. From <a href={`/locations/${location.slug}/roof-inspections`} class="text-violet-600 hover:text-violet-700 font-semibold underline">fast roof inspections</a> to <a href={`/locations/${location.slug}/roof-replacement`} class="text-violet-600 hover:text-violet-700 font-semibold underline">complete roof replacements</a>, we offer the full range of roofing services that {location.name} real estate professionals need. We also specialize in <a href={`/locations/${location.slug}/insurance-claims`} class="text-violet-600 hover:text-violet-700 font-semibold underline">insurance claims assistance</a>, helping your clients navigate the increasingly complex world of roof coverage and depreciation.
        </p>

        <h3>Supporting Both Buyer's and Seller's Agents in {location.name}</h3>
        <p>
          Whether you're representing a buyer who's concerned about roof condition, or a seller who wants to maximize their {location.name} listing's appeal, we have the services and experience to help. Our <a href={`/locations/${location.slug}/pre-listing-services`} class="text-violet-600 hover:text-violet-700 font-semibold underline">pre-listing roof services</a> help sellers identify and address issues before listing, while our payment-at-closing options make it easy for buyers to afford necessary <a href={`/locations/${location.slug}/roof-repair`} class="text-violet-600 hover:text-violet-700 font-semibold underline">roof repairs</a> or replacements in {location.name}.
        </p>

        <h3>Serving {location.name} and Nearby Communities</h3>
        <p>
          In addition to serving {location.name}, we also provide roofing services for realtors throughout the Kansas City metro area. Learn more about our services in {locations.filter(loc => loc.slug !== location.slug).slice(0, 5).map((loc, index, array) => {
            const isLast = index === array.length - 1;
            const isSecondLast = index === array.length - 2;
            return (
              <>
                <a href={`/locations/${loc.slug}`} class="text-violet-600 hover:text-violet-700 font-semibold underline">{loc.name}</a>
                {isSecondLast ? ' and ' : (isLast ? '.' : ', ')}
              </>
            );
          })}
        </p>

        <h3>Ready to Partner with {location.name}'s Trusted Roofing Company?</h3>
        <p>
          Join the hundreds of {location.name} realtors who trust RealtorRoof for their roofing needs. Contact us today to schedule a <a href={`/locations/${location.slug}/roof-inspections`} class="text-violet-600 hover:text-violet-700 font-semibold underline">roof inspection in {location.name}</a> or learn more about our realtor partnership program. Call (913) 268-6052 or request a free estimate online.
        </p>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-16 md:py-24 bg-primary-600">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
        Serving {location.name} Realtors Since 1992
      </h2>
      <p class="text-xl text-primary-100 mb-8 max-w-2xl mx-auto">
        Request a property inspection or schedule a partnership meeting today.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <CTAButton href="/contact" variant="primary" size="lg" class="bg-white text-primary-600 hover:bg-gray-100">
          Request Inspection in {location.name}
        </CTAButton>
        <CTAButton href="tel:9132686052" variant="outline" size="lg" class="border-white text-white hover:bg-primary-700">
          Call (913) 268-6052
        </CTAButton>
      </div>
    </div>
  </section>
</BaseLayout>
