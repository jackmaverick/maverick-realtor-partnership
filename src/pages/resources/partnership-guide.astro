---
import BaseLayout from '../../layouts/BaseLayout.astro';

const partnershipTiers = [
  {
    tier: 'Bronze',
    blurb: 'Fast inspections, clean communication, and marketing assets for new partners.',
    status: 'Application waitlist open'
  },
  {
    tier: 'Silver',
    blurb: 'Dedicated partner manager, expedited crews, and quarterly strategy reviews.',
    status: 'Curating founding partners'
  },
  {
    tier: 'Gold',
    blurb: 'Priority scheduling, co-branded campaigns, and client appreciation events.',
    status: 'Launching with 5 brokerages'
  }
];
---

<BaseLayout
  title="Partnership Guide | Realtor Roof"
  description="We’re finishing the partner tiers, benefits matrix, and onboarding flow you previewed. Join the invite list."
>
  <section class="relative overflow-hidden bg-gradient-to-br from-amber-200 via-orange-200 to-rose-200 py-16 md:py-24">
    <div class="absolute inset-0 bg-gradient-to-tr from-amber-300/50 via-transparent to-rose-400/40"></div>
    <div class="relative mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 text-center">
      <span class="mb-4 inline-flex items-center gap-2 rounded-full border border-white/60 bg-white/70 px-4 py-2 text-sm font-semibold text-amber-700 backdrop-blur">
        🤝 Partnership Program Refresh
      </span>
      <h1 class="mb-4 text-4xl font-bold text-gray-900 sm:text-5xl">
        New Partner Tiers Launching Soon
      </h1>
      <p class="mx-auto mb-8 max-w-2xl text-lg text-gray-700">
        The full Bronze, Silver, and Gold tier experience—pricing guidance, task automations, marketing perks—is in final polish. We’re onboarding the first cohort now.
      </p>
      <div class="flex flex-col items-center justify-center gap-3 sm:flex-row">
        <a
          href="/contact?topic=partnership-waitlist"
          class="inline-flex items-center gap-2 rounded-xl bg-white px-6 py-3 text-sm font-semibold text-amber-700 shadow-lg transition hover:bg-amber-50"
        >
          Join the Partner Waitlist
          <span>→</span>
        </a>
        <a
          href="tel:9132988759"
          class="inline-flex items-center gap-2 rounded-xl border border-white/60 px-6 py-3 text-sm font-semibold text-white transition hover:bg-white/10"
        >
          Talk Through Requirements
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
          </svg>
        </a>
      </div>
    </div>
  </section>

  <section class="bg-white py-16">
    <div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
      <div class="mb-8 text-center">
        <h2 class="text-3xl font-bold text-gray-900">Snapshot of the Tiers</h2>
        <p class="mt-3 text-lg text-gray-600">
          The cards below mirror the partner matrix design you saw—we’re keeping your favorite pieces while we wire up the onboarding workflow.
        </p>
      </div>

      <div class="grid gap-6 md:grid-cols-3">
        {partnershipTiers.map((tier) => (
          <div class="rounded-3xl border border-amber-100 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-xl">
            <div class="mb-4 inline-flex h-12 w-12 items-center justify-center rounded-2xl bg-gradient-to-br from-amber-400 to-rose-400 text-xl text-white">
              ⭐
            </div>
            <h3 class="text-2xl font-semibold text-gray-900">{tier.tier} Partner</h3>
            <p class="mt-3 text-sm text-gray-600">{tier.blurb}</p>
            <p class="mt-5 text-xs font-semibold uppercase tracking-wide text-amber-600">{tier.status}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
